import 'package:flutter/material.dart';

/// African Heritage Application Color Palette
/// Inspired by traditional African earth tones and cultural aesthetics
class AppColors {
  // Primary colors - Earth tones foundation
  static const Color primary = Color(0xFF4B2A0A);           // Marron foncé - terre, tradition, stabilité
  static const Color primaryLight = Color(0xFF6B3F1A);     // Version plus claire du marron
  static const Color primaryDark = Color(0xFF2A1505);      // Version plus foncée du marron
  
  // Secondary colors - Warm earth accents
  static const Color secondary = Color(0xFFC35A1E);        // Orange terre cuite - culture, énergie
  static const Color secondaryLight = Color(0xFFD67A3E);   // Version plus claire
  static const Color secondaryDark = Color(0xFFA3450F);    // Version plus foncée
  
  // Accent colors - African warmth
  static const Color accent = Color(0xFFDDA63A);           // Ocre jaune - soleil, motifs africains
  static const Color accentSecondary = Color(0xFFA64521);  // Rouge argile - chaleur, éléments visuels
  static const Color accentLight = Color(0xFFE8B654);      // Version plus claire de l'ocre
  static const Color accentDark = Color(0xFFC4901A);       // Version plus foncée de l'ocre
  
  // Background and surface colors
  static const Color background = Color(0xFFF7F1E8);       // Beige clair - fond naturel, lisibilité
  static const Color surface = Color(0xFFFFFFFF);          // Blanc pur pour les cartes
  static const Color surfaceVariant = Color(0xFFF0E6D6);   // Variante du beige pour les éléments
  
  // Text colors - Contraste optimal
  static const Color textPrimary = Color(0xFF231F20);      // Noir profond - textes principaux
  static const Color textSecondary = Color(0xFF4B2A0A);    // Marron foncé - textes secondaires
  static const Color textLight = Color(0xFF8B7355);        // Texte plus clair
  static const Color textOnPrimary = Color(0xFFF7F1E8);    // Texte sur fond sombre
  
  // Interactive elements
  static const Color buttonPrimary = Color(0xFF4B2A0A);    // Boutons principaux
  static const Color buttonSecondary = Color(0xFFC35A1E);  // Boutons secondaires
  static const Color buttonAccent = Color(0xFFDDA63A);     // Boutons d'accent
  
  // Status colors adapted to African palette
  static const Color success = Color(0xFF2D5A27);          // Vert terre
  static const Color warning = Color(0xFFDDA63A);          // Ocre (réutilisé)
  static const Color error = Color(0xFFA64521);            // Rouge argile (réutilisé)
  static const Color info = Color(0xFF4B6B8A);            // Bleu terreux
  
  // Shadows and overlays
  static const Color shadow = Color(0x1A231F20);           // Ombre subtile
  static const Color overlay = Color(0x4D231F20);          // Overlay sombre
  static const Color divider = Color(0x33231F20);          // Séparateurs
  static const Color border = Color(0x1A4B2A0A);           // Bordures subtiles
}